import { redirect } from 'next/navigation';

// Consolidated into /mothers-day-gifts — better page with hero image, full product catalog, and editorial content
export default function GiftIdeasForMomPage() {
  redirect('/mothers-day-gifts');
}
